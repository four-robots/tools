/**
 * Search Page Loading UI
 * 
 * Loading state for the search page route
 */

import { SearchLoading } from '@/components/search/common';

export default function Loading() {
  return (
    <SearchLoading message="Loading search page..." />
  );
}